<?php
  /*
  |------------------------------------------------------------------
  | 날짜/시간 처리 - 학습 과정 (주석처리, 실제 사용 안함)
  |------------------------------------------------------------------
  */
  // [ STEP 1 ] 날짜/시간 각각 따로 구하기 -------------------------
  // $date = date("Y/m/d");
  // date() = PHP 내장 날짜함수
  // "Y" = 4자리 연도 (2026)
  // "m" = 2자리 월 (03)
  // "d" = 2자리 일 (28)
  // 결과: "2026/03/28"
  // $hour   = date("H");  // "H" = 24시간제 시 (00~23)
  // $minute = date("i");  // "i" = 분 (00~59) ← m이 month랑 겹쳐서 관례상 i
  // $second = date("s");  // "s" = 초 (00~59)

  // [ STEP 2 ] AM/PM 직접 계산 ------------------------------------
  // if ($hour < 12 || $hour == 24) {
  // ⚠️ 버그: $hour == 24 는 date("H") 에서 절대 안나옴 (범위: 0~23)
  //     $am_pm = "AM";
  // } else {
  //     $am_pm = "PM";
  //     $hour  = $hour - 12;
  //     // 13시 → 1시 변환
  //     // ⚠️ 버그: 12시 정오도 -12 돼서 0시로 잘못 출력됨
  // }

  // $time = $hour . ":" . $minute . ":" . $second . "" . $am_pm;
  // .  = 문자열 이어붙이기 (JS 의 + 와 같음)
  // "" = ⚠️ 빈문자열 연결, 의미없는 코드

  // echo "현재 접속 일시 : " . $date . " " . $time;
  // ↑ 위 방식 대신 아래 date() 포맷으로 한번에 처리

  /*
  |------------------------------------------------------------------
  | 날짜/시간 처리
  |------------------------------------------------------------------
  | date() 하나로 모든 포맷 한번에 처리
  | Y/m/d = 날짜, " " = 공백, h = 12시간제, i = 분, s = 초, A = AM/PM
  |------------------------------------------------------------------
  */

  $datetime = date("Y/m/d h:i:s A");
  $datetime = str_replace(["AM", "PM"], ["오전", "오후"], $datetime);

?>